-r requirements.txt
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27
aiosqlite>=0.20
ruff>=0.6
pre-commit>=3.8

# Development-only: route LLM calls through a Claude Pro/Max subscription
# (CLAUDE_CODE_OAUTH_TOKEN) instead of a paid API key. See app/core/llm.py.
claude-agent-sdk>=0.1.0
